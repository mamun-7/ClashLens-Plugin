<Window x:Class="ClashSolver.UI.ValidationResolution.ReviewIssues.WndAIResolve"
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			xmlns:local="clr-namespace:ClashSolver.UI.ValidationResolution.ReviewIssues"
			mc:Ignorable="d" 
			Title="Resolutions"
			Width="800" Height="450"
			MinWidth="600" MinHeight="300"
			d:DesignHeight="450" d:DesignWidth="800">

	<Window.Resources>
		<ResourceDictionary Source="pack://application:,,,/ClashSolver.UI;component/Common/Generic.xaml"/>
	</Window.Resources>

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!-- Consultant Links Section -->
		<GroupBox
							Grid.Row="0"
							Header="Solutions:"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Stretch">
			<Grid>
				<!-- Data Grid -->
				<DataGrid x:Name="gridResolves" Grid.Column="1" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding Resolves}" SelectedItem="{Binding SelectedResolve}" SelectionMode="Single">
					<DataGrid.Resources>
						<Style TargetType="DataGridColumnHeader">
							<Setter Property="HorizontalContentAlignment" Value="Center"/>
						</Style>
					</DataGrid.Resources>
					<DataGrid.Columns>
						<DataGridTextColumn Header="No" Width="50" Binding="{Binding No}" IsReadOnly="True" />
						<DataGridTextColumn Header="Category" Width="100" Binding="{Binding Category.Name}" IsReadOnly="True"/>
						<DataGridTextColumn Header="TargetId" Width="100" Binding="{Binding TargetId}" IsReadOnly="True"/>
						<DataGridTextColumn Header="Description" Width="300" Binding="{Binding Description}" IsReadOnly="True"/>
					</DataGrid.Columns>
				</DataGrid>
			</Grid>
		</GroupBox>

		<!-- Footer Buttons -->
		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
			<Button Name="btnOK" Content="OK" Style="{StaticResource FooterButtonStyle}" Click="btnOK_Click"/>
			<Button Name="btnCancel" Content="Cancel" Style="{StaticResource FooterButtonStyle}" Click="btnCancel_Click"/>
		</StackPanel>
	</Grid>
</Window>
